// Amount of milliseconds for one loss of friend strength
const TIME_PER_STRENGTH_LOSS = 60500000;
// Amount of milliseconds (24 hours) before token expires
const TIME_FOR_TOKEN_EXPIRE = 8.64e7;

module.exports = {
  TIME_PER_STRENGTH_LOSS,
  TIME_FOR_TOKEN_EXPIRE
};
